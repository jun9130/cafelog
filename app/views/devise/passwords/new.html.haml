.devise.container.my-5
  %h1.devise__header.text-center.py-5 パスワードの再設定
  = form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
    = render "devise/shared/error_messages", resource: resource
    .devise__form-group.mx-auto.col-4
      = f.label :email, "メールアドレス", class: "devise__form-label"
      = f.email_field :email, autofocus: true, autocomplete: "email", class: 'devise__form-input'
    .devise__form-group.mx-auto.col-4.mt-5
      = f.submit ("送信"), class: 'devise-button'
    .devise__form-group.mx-auto.col-4.py-5
      = render "devise/shared/links"
