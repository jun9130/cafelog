.devise.container.my-5
  %h1.devise__header.text-center.py-5 ログイン
  = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
    .devise__form-group.mx-auto.col-4 
      = f.label :email, "メールアドレス", class: "devise__form-label"
      = f.email_field :email, autofocus: true, autocomplete: 'email', class: 'devise__form-input'
    .devise__form-group.mx-auto.col-4 
      = f.label :password, "パスワード", class: "devise__form-label"
      = f.password_field :password, autocomplete: 'current-password', class: 'devise__form-input' 
    - if devise_mapping.rememberable?
      .devise__form-group.mx-auto.col-4.form-check
        = f.check_box :remember_me, class: 'form-check-input'
        = f.label :remember_me, class: 'form-check-label' do
          = resource.class.human_attribute_name('remember_me')
      .devise__form-group.mx-auto.col-4.mt-5
        = f.submit  ('ログイン'), class: 'devise-button'
    .devise__form-group.mx-auto.col-4.mt-3
      = form_for(User.new, as: :user, url: session_path(:user)) do |f|
        = f.hidden_field :email, value: "test@gmail.com"
        = f.hidden_field :password, value: "test123456"
        = f.submit "かんたんログイン", class: 'devise-button'
    .devise__form-group.mx-auto.py-5
      = render "devise/shared/links"